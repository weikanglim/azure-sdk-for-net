#!/usr/bin/env bash
set -euo pipefail

export PATH="$HOME/.local/bin:$HOME/.dotnet/tools:$PATH"
export DOTNET_ROOT="$HOME/.dotnet"

echo "==> Checking Azure SDK for .NET orb toolchains"
dotnet --version
node --version
pwsh -NoLogo -NoProfile -Command '$PSVersionTable.PSVersion.ToString()'
